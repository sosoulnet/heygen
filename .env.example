# HeyGen login credentials (optional — you can log in manually if not set)
HEYGEN_EMAIL=your_email@example.com
HEYGEN_PASSWORD=your_password_here

# Set to "1" to run browser in headless mode (no visible window)
HEYGEN_HEADLESS=0
